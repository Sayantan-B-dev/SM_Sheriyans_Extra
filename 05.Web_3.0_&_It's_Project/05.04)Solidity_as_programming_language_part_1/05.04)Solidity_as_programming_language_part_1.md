# 1️⃣ Remix & Solidity Basics

### 🔹 `remix.ethereum.org`

* Browser-based **Solidity IDE**
* No setup required
* Used for **learning, testing, and small demos**
* Automatically:

  * Compiles contracts
  * Connects to test networks
  * Shows deployed contract UI

---

# 2️⃣ ABI (Application Binary Interface)

### 🔹 What is ABI?

Think of ABI as:

> **The “executable + instruction manual” for your smart contract**

* Describes:

  * Functions
  * Inputs & outputs
  * Types
* Frontends (JS, Web3, Ethers) **cannot talk to contracts without ABI**
* Generated automatically after compilation

📌 **Analogy**

| Programming         | Blockchain        |
| ------------------- | ----------------- |
| `.exe` file         | Deployed bytecode |
| Function signatures | ABI               |

---

# 3️⃣ Solidity File & Contract Structure

### 🔹 File Extension

```text
Demo.sol
```

### 🔹 Pragma

```solidity
pragma solidity ^0.8.13;
```

* Tells compiler version
* `^` = compatible with future non-breaking versions

### 🔹 Contract Name Rule

```solidity
contract Demo {
}
```

⚠️ **Best practice**: Contract name = filename
(Not mandatory, but industry standard)

---

# 4️⃣ Solidity Data Types (Core)

## 🔹 Integers (`uint`, `int`)

### What they are

* `uint` → **unsigned integer** (0 → ∞)
* `int` → **signed integer** (- → +)

```solidity
uint public age = 18;
int public balance = -10;
```

### Sizes

```solidity
uint8   // 0 to 255
uint256 // default (most used)
```

### Tricky Points

* `uint` **cannot be negative**
* Overflow/underflow **auto-reverts** in Solidity ≥0.8
* Smaller sizes don’t always save gas (packing matters)

### Getter Function (manual)

```solidity
function getAge() public view returns (uint) {
    return age;
}
```

📌 **Note**: `public` variables already auto-create getters.

---

## 🔹 Strings

### What they are

* Dynamic UTF-8 encoded text
* Stored as **bytes internally**

```solidity
string public name = "sam";
```

### Returning strings

```solidity
function getName() public view returns (string memory) {
    return name;
}
```

### Tricky Points

* Strings are **expensive**
* Cannot compare directly using `==`
* Must specify **memory** when used in functions

### Memory keyword

```solidity
string memory _name
```

* Temporary
* Cheaper than storage

---

## 🔹 Address

### What it is

* 20-byte Ethereum account or contract

```solidity
address public userAddress;
```

### Capturing caller address

```solidity
function getAddress() public {
    userAddress = msg.sender;
}
```

### Tricky Points

* `msg.sender` = caller
* Contract addresses also have addresses
* Address ≠ string

### Payable Address

```solidity
address payable receiver;
```

---

## 🔹 Input Variables

```solidity
function setName(string memory _name) public {
    name = _name;
}
```

### Convention

* `_name` → input variable
* Avoid name collision with state variables

---

# 5️⃣ Console Logging (Hardhat Only)

```solidity
import "hardhat/console.sol";
```

```solidity
console.log("hi");
```

### Important

* ❌ **NOT supported on mainnet**
* ❌ **NOT supported in Remix**
* ✅ Only for **local Hardhat testing**

📌 Used only for debugging while development

---

# 6️⃣ Mapping

```solidity
mapping(string => uint) public student;
```

### What it is

* Key → Value storage
* Hash table
* No length
* No iteration

```solidity
function setEntry(string memory _name, uint _age) public {
    student[_name] = _age;
}
```

### Tricky Points

* Default value always exists
* Cannot check if key exists
* Gas efficient
* Cannot loop

---

# 7️⃣ Structs & Arrays

### Struct

```solidity
struct Person {
    uint age;
    string name;
    uint salary;
}
```

### Storage

```solidity
Person public obj;
Person[] public arr;
```

### Function

```solidity
function setPerson(
    uint _age,
    string memory _name,
    uint _salary
) public {
    obj = Person(_age, _name, _salary);
    arr.push(obj);
}
```

### Tricky Points

* Structs are **custom data types**
* Arrays grow dynamically
* Returning arrays of structs is expensive
* Use mappings for large data

---

# 8️⃣ Function Modifiers (VERY IMPORTANT)

---

## 🔹 `view`

### Meaning

* Reads blockchain state
* ❌ Cannot modify data
* ❌ No gas **when called externally**
* ✅ Gas **if called internally**

```solidity
function getAge() public view returns (uint) {
    return age;
}
```

### Use cases

* Getters
* Reading balances
* Fetching stored data

---

## 🔹 `pure`

### Meaning

* Does NOT read
* Does NOT write
* Works only with inputs

```solidity
function add(uint a, uint b) public pure returns (uint) {
    return a + b;
}
```

### Use cases

* Math
* Validation logic
* Utilities

### Tricky Point

* Even reading a state variable breaks `pure`

---

## 🔹 `payable`

### Meaning

* Allows function to **receive ETH**

```solidity
function deposit() public payable {
}
```

### Access ETH amount

```solidity
msg.value
```

### Use cases

* Payments
* Donations
* Escrow
* Fees

### Tricky Points

* Without `payable`, ETH transfer **reverts**
* ETH stored in `address(this).balance`

---

# 9️⃣ Gas & Transactions Summary

| Action          | Gas |
| --------------- | --- |
| view (external) | ❌   |
| pure            | ❌   |
| state change    | ✅   |
| payable         | ✅   |
| deploy          | ✅   |

---

# 🔟 EVM Version in Remix

When Remix shows **EVM version** after compilation:

* Bytecode is generated
* Contract is deployable
* Compatible with selected network

---

# 🔚 Final Notes (Important)

* `public` variables auto-generate getters
* State variables cost gas
* Memory is temporary
* Storage is permanent
* Testnets ≠ real money
* SepETH is only for testing

---