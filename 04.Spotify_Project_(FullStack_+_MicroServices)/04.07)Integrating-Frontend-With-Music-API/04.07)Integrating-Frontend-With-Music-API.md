```markdown
### ▶ Back to Frontend — Artist Dashboard Routing
➡️ tell AI:
   `now setup the react router dom for the route /artist/dashboard`
```

```markdown
### ▶ Artist Dashboard File Structure
➡️ AI should create `ArtistDashboard.jsx`
➡️ move it into `artist/` folder
➡️ remove unnecessary boilerplate from `App.jsx`
```

```markdown
### ▶ Artist Dashboard UI Sections
➡️ next AI prompt:
   `add the musics and playlists section on the artist dashboard page use proper css for this make responsive also`
```

```markdown
### ▶ Playlist & Music Data Shape
➡️ next AI prompt:
   `playlists we have data like title, artist, musics and in the music object we have data like title, artist, coverImageUrl, musicUrl`
```

```markdown
### ▶ State & Imports Cleanup
➡️ in `ArtistDashboard.jsx`
➡️ import `axios` and `useEffect` from react
➡️ remove any dummy music added by AI
➡️ use `useState` to initialize dummy state manually
➡️ do the same for playlists
```

```markdown
### ▶ Fetch Artist Musics
➡️ use `useEffect`
➡️ call:
   `axios.get('/api/music/artist-musics', { withCredentials: true })`

➡️ in `.then()`:
   setMusic(
     res.data.musics.map(m => ({
       id,
       title,
       artist,
       coverImageUrl,
       musicUrl,
       plays: default,
       duration: default,
       released: default
     }))
   )
```

```markdown
### ▶ Enable CORS (Music Service Backend)
➡️ in `music-service`
➡️ npm i cors
➡️ in `app.js`:
   - import `cors`
   - use cors middleware after `express()`

➡️ basic setup:
   origin + credentials enabled
```

```markdown
### ▶ Google Auth Redirect (Auth Service)
➡️ in `auth.controller.js`
➡️ inside `googleAuthCallback`
➡️ while checking `isUserAlreadyExist`:
   - if `user.role === 'artist'`
   - redirect to `<frontend-url>/artist/dashboard`
```

```markdown
### ▶ Artist Playlist API (Backend)
➡️ in `music.routes.js`
➡️ create GET `/playlist/artist`
➡️ use `authArtistMiddleware`
➡️ call `getArtistPlaylist` controller
```

```markdown
### ▶ getArtistPlaylist Controller
➡️ in `music.controller.js`
➡️ create `getArtistPlaylist`
➡️ inside try block:
   - find playlists by `artistId: req.user.id`
➡️ return playlists
```

```markdown
### ▶ Fetch Artist Playlists (Frontend)
➡️ in `ArtistDashboard.jsx`
➡️ add another `useEffect`
➡️ axios GET:
   `/api/music/playlist/artist`
   with `{ withCredentials: true }`

➡️ in `.then()`:
   setPlaylists(
     res.data.playlists.map(p => ({
       id,
       title,
       artist,
       followers: default,
       updated: default,
       musics: default
     }))
   )

➡️ make sure to use `[]` dependency array
```

```markdown
### ▶ Upload Music Route (Frontend)
➡️ next AI prompt:
   `now create the /artist/dashboard/upload-music route this page contains a form with input fields music[file], coverImage[file], title, create the proper UI with responsive css`
```

```markdown
### ▶ Navigation to Upload Page
➡️ in `ArtistDashboard.jsx`
➡️ import `useNavigate`
➡️ on button click:
   navigate to `/artist/dashboard/upload-music`
```

```markdown
### ▶ Upload Music Preview
➡️ in `UploadMusic.jsx`
➡️ AI prompt:
   `add the preview image and music functionality`
```

```markdown
### ▶ Upload Music Submit Logic
➡️ in `UploadMusic.jsx`
➡️ inside `handleSubmit`:

➡️ create:
   `const formData = new FormData()`

➡️ append:
   - `title`
   - `coverImage` (if exists)
   - `music` (if exists)

➡️ import `axios` and `useNavigate`
➡️ axios POST:
   `/api/music/upload`
   with `formData`
   and `{ withCredentials: true }`

➡️ on success:
   navigate to `/artist/dashboard`
```

```markdown
### ▶ Home Page Creation
➡️ back to `Home.jsx`
➡️ AI prompt:
   `now create the home.jsx this page will show playlists and musics`
```

```markdown
### ▶ Home Page Data Handling
➡️ remove `playlist.coverImageUrl` if AI adds it
➡️ import:
   - useEffect
   - useState
   - useNavigate
   - axios

➡️ initialize dummy state
➡️ inside `useEffect`:

➡️ axios GET `/api/music`
   withCredentials
   then:
   setMusics(res.data.musics.map(m => ({ all info })))

➡️ axios GET `/api/music/playlists`
   withCredentials
   then:
   setPlaylists(res.data.playlists.map(p => ({ all info })))
```

```markdown
### ▶ CSS Fixes
➡️ adjust or clean CSS if needed
➡️ keep layout responsive
```

```markdown
### ▶ Music Player Route
➡️ AI prompt:
   `now create a route /music/:id in this page create the full music controls like volume and speed bar also show the cover image`
```

```markdown
### ▶ Navigate to Music Player
➡️ in `Home.jsx`
➡️ on music card click:
   navigate to `/music/${m.id}`
```

```markdown
### ▶ Music Player Data Fetch
➡️ in `MusicPlayer.jsx`
➡️ import `axios` and `useNavigate`
➡️ create `useState(track)`

➡️ if `!track` → return loading

➡️ before render:
   useEffect with dependency array
   axios GET:
   `/api/music/get-details/${id}`

➡️ set track from response
```
